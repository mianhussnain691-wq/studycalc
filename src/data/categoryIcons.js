import { Calculator, ClipboardList, GraduationCap, CalendarCheck, PiggyBank } from "lucide-react";

// Maps the `icon` string stored in categories.js to the actual lucide-react
// component, so category data can stay plain, serializable strings.
const categoryIcons = {
  Calculator,
  ClipboardList,
  GraduationCap,
  CalendarCheck,
  PiggyBank,
};

export default categoryIcons;
