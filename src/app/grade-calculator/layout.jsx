export const metadata = {
  title: "Grade Calculator | StudyCalc",
  description:
    "Find your grade, GPA points and academic performance instantly.",
};

export default function GradeLayout({ children }) {
  return children;
}