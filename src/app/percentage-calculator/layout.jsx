export const metadata = {
  title: "Percentage Calculator | StudyCalc",
  description:
    "Convert obtained marks into percentage instantly using StudyCalc.",
};

export default function PercentageLayout({ children }) {
  return children;
}